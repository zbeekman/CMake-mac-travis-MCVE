---
language: generic

os: osx
env:
  - CC=gcc-8
addons:
  homebrew:
    packages:
      - gcc
      - cmake
      - open-mpi
    update: true

before_install: brew cask rm oclint
install: brew link gcc
script: cmake .
